[
  {
    "name": "theme_info",
    "theme_name": "WHANE",
    "theme_version": "1.0.0",
    "theme_author": "WHANE",
    "theme_documentation_url": "https://help.shopify.com",
    "theme_support_url": "https://help.shopify.com"
  },
  {
    "name": "Brand",
    "settings": [
      {
        "type": "header",
        "content": "Two-color system (do not add a third)"
      },
      {
        "type": "color",
        "id": "color_cobalt",
        "label": "Cobalt (signature)",
        "default": "#1B2BC4"
      },
      {
        "type": "color",
        "id": "color_white",
        "label": "White (base)",
        "default": "#FFFFFF"
      },
      {
        "type": "header",
        "content": "Identity"
      },
      {
        "type": "text",
        "id": "wordmark",
        "label": "Wordmark (text fallback)",
        "default": "WHANE.",
        "info": "Always uppercase, always with the trailing period."
      },
      {
        "type": "checkbox",
        "id": "wordmark_use_svg",
        "label": "Use SVG wordmark",
        "default": false,
        "info": "Upload the logo as assets/wordmark.svg, then enable. It is always rendered in cobalt."
      },
      {
        "type": "image_picker",
        "id": "favicon",
        "label": "Favicon"
      }
    ]
  },
  {
    "name": "Navigation",
    "settings": [
      {
        "type": "paragraph",
        "content": "The menu band (every page except the homepage) and the homepage nav use these three links. For each URL field, click it and SELECT the page from the dropdown (Pages → Shop / Lookbook / Info). Do not type the URL by hand — the picker always uses the correct address even if the page handle differs."
      },
      { "type": "text", "id": "nav_shop_label", "label": "Link 1 label", "default": "Shop" },
      { "type": "url", "id": "nav_shop_link", "label": "Link 1 URL — select the Shop page" },
      { "type": "text", "id": "nav_lookbook_label", "label": "Link 2 label", "default": "Lookbook" },
      { "type": "url", "id": "nav_lookbook_link", "label": "Link 2 URL — select the Lookbook page" },
      { "type": "text", "id": "nav_info_label", "label": "Link 3 label", "default": "Info" },
      { "type": "url", "id": "nav_info_link", "label": "Link 3 URL — select the Info page" }
    ]
  }
]
